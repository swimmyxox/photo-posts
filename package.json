{
  "name": "photo-posts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.14.1"
  },
  "scripts": {
    "build": "node-sass --output css --include-path scss scss/main.scss",
    "build:watch": "yarn build --watch"
  }
}
